services:
  dev:
    # docker compose run --rm dev python grid_settings.py
    # docker compose run --rm dev python grid_basket_calc.py --entry 150.00 --price 149.00 --direction buy
    image: ${REGISTRY}/fx-utils:dev
    build:
      context: ./app
      dockerfile: Dockerfile.dev
    tty: true
    volumes:
      - "./app:/app"
      - "./data:/data"
